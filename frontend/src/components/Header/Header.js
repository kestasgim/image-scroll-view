import React from 'react';

import './Header.css';

const Header = props => {
  return (
    <header className="header">
      <h1>Image scroll view</h1>
    </header>
  );
};

export default Header;
